@* @model int

<style>
    .stepper {
        display: flex;
        justify-content: center;
        align-items: center;
        gap: 18px;
        margin: 25px 0;
        flex-wrap: wrap;
        direction: rtl;
    }

    .step {
        display: flex;
        align-items: center;
        gap: 8px;
        font-weight: 600;
        font-size: 14px;
        color: #94a3b8;
        position: relative;
    }

    .step-circle {
        width: 32px;
        height: 32px;
        border-radius: 50%;
        background: #e2e8f0;
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 13px;
        font-weight: bold;
        color: #64748b;
    }

    .step.active {
        color: #0E7490;
    }

        .step.active .step-circle {
            background: linear-gradient(135deg, #0E7490, #0891B2);
            color: white;
            box-shadow: 0 6px 15px rgba(14,116,144,0.25);
        }

    .step.completed .step-circle {
        background: #10b981;
        color: white;
    }

    .step-line {
        width: 45px;
        height: 2px;
        background: #e2e8f0;
    }

    @@media(max-width:768px) {
        .stepper {
            gap: 10px;
        }

        .step {
            font-size: 12px;
        }

        .step-line {
            width: 25px;
        }
    }
</style>

<div class="stepper">

    <div class="step @(Model >= 1 ? "active" : "")">
        <div class="step-circle">1</div>
        الخدمة
    </div>

    <div class="step-line"></div>

    <div class="step @(Model >= 2 ? "active" : "")">
        <div class="step-circle">2</div>
        الطبيب
    </div>

    <div class="step-line"></div>

    <div class="step @(Model >= 3 ? "active" : "")">
        <div class="step-circle">3</div>
        الموعد
    </div>

    <div class="step-line"></div>

    <div class="step @(Model >= 4 ? "active" : "")">
        <div class="step-circle">4</div>
        بيانات المريض
    </div>

    <div class="step-line"></div>

    <div class="step @(Model >= 5 ? "active" : "")">
        <div class="step-circle">5</div>
        التأكيد
    </div>

</div> *@

@model int

<style>
    .stepper-vertical {
        display: flex;
        flex-direction: column;
        gap: 18px;
        margin: 25px 0;
        direction: rtl;
    }

    .step {
        display: flex;
        align-items: flex-start;
        gap: 14px;
        position: relative;
    }

        /* الخط بين الخطوات */
        .step:not(:last-child)::after {
            content: "";
            position: absolute;
            top: 38px;
            right: 15px;
            width: 2px;
            height: 35px;
            background: #e2e8f0;
        }

    /* الدائرة */
    .step-circle {
        width: 32px;
        height: 32px;
        border-radius: 50%;
        background: #e2e8f0;
        color: #64748b;
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 13px;
        font-weight: bold;
        flex-shrink: 0;
        z-index: 2;
    }

    /* النص */
    .step-content {
        display: flex;
        flex-direction: column;
        gap: 2px;
    }

    .step-title {
        font-size: 15px;
        font-weight: 700;
        color: #94a3b8;
    }

    .step-subtitle {
        font-size: 12px;
        color: #cbd5e1;
    }

    /* Active */
    .step.active .step-circle {
        background: linear-gradient(135deg, #0E7490, #0891B2);
        color: white;
        box-shadow: 0 6px 15px rgba(14,116,144,0.25);
    }

    .step.active .step-title {
        color: #0f172a;
    }

    .step.active .step-subtitle {
        color: #64748b;
    }

    /* Completed */
    .step.completed .step-circle {
        background: #10b981;
        color: white;
    }

    /* Card */
    .stepper-wrapper {
        background: #fff;
        border-radius: 22px;
        padding: 22px;
        border: 1px solid #edf2f7;
        box-shadow: 0 8px 25px rgba(15,23,42,0.05);
        margin-top:40px;
        padding-bottom: 130px;
    }
</style>

<div class="stepper-wrapper">

    <div class="stepper-vertical">

        <!-- الخدمة -->
        <div class="step @(Model >= 1 ? "active" : "")">
            <div class="step-circle">1</div>

            <div class="step-content">
                <div class="step-title">اختيار الخدمة</div>
                <div class="step-subtitle">
                    اختر الخدمة الطبية المناسبة
                </div>
            </div>
        </div>

        <!-- الطبيب -->
        <div class="step @(Model >= 2 ? "active" : "")">
            <div class="step-circle">2</div>

            <div class="step-content">
                <div class="step-title">اختيار الطبيب</div>
                <div class="step-subtitle">
                    اختر الطبيب المناسب للحجز
                </div>
            </div>
        </div>

        <!-- الموعد -->
        <div class="step @(Model >= 3 ? "active" : "")">
            <div class="step-circle">3</div>

            <div class="step-content">
                <div class="step-title">اختيار الموعد</div>
                <div class="step-subtitle">
                    حدد اليوم والوقت المناسب
                </div>
            </div>
        </div>

        <!-- بيانات المريض -->
        <div class="step @(Model >= 4 ? "active" : "")">
            <div class="step-circle">4</div>

            <div class="step-content">
                <div class="step-title">بيانات المريض</div>
                <div class="step-subtitle">
                    أدخل بيانات الحجز المطلوبة
                </div>
            </div>
        </div>

        <!-- التأكيد -->
        <div class="step @(Model >= 5 ? "active" : "")">
            <div class="step-circle">5</div>

            <div class="step-content">
                <div class="step-title">تأكيد الحجز</div>
                <div class="step-subtitle">
                    مراجعة وتأكيد تفاصيل الحجز
                </div>
            </div>
        </div>

    </div>

</div>